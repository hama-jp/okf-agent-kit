# CLAUDE.md / AGENTS.md への常設追記例

毎回プロンプトに書かなくても効くように、プロジェクトの指示ファイルに数行入れておく形です。

```markdown
## ノートの規約 (OKF v0.2 語彙)

- knowledge/ の新規ノートには OKF v0.2 語彙の frontmatter
  (type / generated / verified / status / stale_after) を付ける
- status は draft から始める (無指定 = stable 扱いのため)
- 検証イベント (verified) は、実際に突き合わせたときにその場で追記する。
  確認していない検証イベントは書かない
- 書き手・確認者の表記: human:<id> / <producer>/<version> / process:<id>。
  新しい表記を使うときは actor 一覧表に追記してから使う
```

## 運用のコツ

- 最初から全ファイルに広げず、「エージェントに書かせて溜まる文書」
  (調査ノート・実験記録・台帳) だけに絞ると、書く負担と効果のバランスが取れます。
- 過去ファイルへの一括後付けはおすすめしません。ファイル内に痕跡が無い検証イベントは
  後からは書けない (書けば捏造になる) ので、新規作成分から前向きに始めるのが実務的です。
